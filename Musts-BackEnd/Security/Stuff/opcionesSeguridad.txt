Cómo trabajar la seguridad, de peor a mejor:
*la que vimos en la entrega anterior.
*igual que la anterior, pero encriptando la info en base.
*así, pero validándolo en un middleware.
*así, pero con login y un token temporal que sea un random string que se guarda encriptado en base.
*así, pero el token temporal no es un random string que se guarda en base, es un token informativo que depende
de una clave secreta y se puede validar sin consultar a base.
*así, pero el token es un JWT.

*que los roles y permisos se consulten y guarden en base, a mano.
*que los roles y permisos se verifiquen a partir del HTTP User object y de lo que trae ya .NET (FWK de
Middlewares y decoradores Authorization, Rol, etcs) sin necesidad de ir a base.
*igual ojo, que los roles y autorizaciones estén definidos en base tiene sus ventajas. no dependés de esa API.

De todo esto, que investiguen y elijan lo que puedan/prefieran.